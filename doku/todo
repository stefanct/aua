mmu ordentlich machen :) inkl sram controller, rom in ram
jmpl/ret konkretisieren/durchdenken und dann implementieren (as only. abi (argument registers, caller/callee saves etc))
ex fwd nochmal ueberdenken
(uart blocking io - fuer unnoetig befunden)
alu optimieren
ganz tolle neue instructions erfinden
bootloader
i-cache

doku (siehe http://en.wikiversity.org/wiki/Computer_Architecture_Lab#Documentation)
